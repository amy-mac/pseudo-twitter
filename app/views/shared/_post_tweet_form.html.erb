<%= form_for @tweet do |f| %>
  <%= f.text_area :tweet_text, class: 'radius', rows: 10, placeholder: 'Compose your tweet', required: true %>
  <%= f.submit 'Tweet', class: 'button button-primary radius' %>
<% end %>
